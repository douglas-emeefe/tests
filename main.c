#include <stdio.h>
#include <locale.h>

int main()
{
    printf("Criado por: Douglas Oliveira\n\n");
    int a = 10, b = 20, soma;

    soma = a + b;

    printf("%d + %d = %d\n", a, b, soma);

    return 0;
}